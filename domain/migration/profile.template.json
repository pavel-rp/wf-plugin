{
  "stack": {
    "source-root": "<SOURCE_ROOT: repo-relative root of the legacy source side, forward slashes — e.g. src/Legacy.Web>",
    "target-root": "<TARGET_ROOT: repo-relative root of the ported target side, forward slashes — e.g. src/app>",
    "source-exts": ["<SOURCE_EXT: a source file extension, e.g. .cs — list one per mappable source file type>"],
    "target-exts": ["<TARGET_EXT: a target file extension, e.g. .ts — list one per target file type>"]
  },
  "type-map": [
    {
      "source-type": "<SOURCE_TYPE: a source-language type token, e.g. int>",
      "target-type": "<TARGET_TYPE: its target-language counterpart, e.g. number>",
      "nullable": false,
      "note": "<FILL: add one row per mappable source type; set nullable:true for the nullable form; use this note for round-trip / collection behaviour. Worked example: domain/migration/fixtures/valid-profile.json>"
    }
  ],
  "invariants": [
    {
      "id": "<INVARIANT_ID: stable identifier, e.g. names-preserved>",
      "statement": "<FILL: the cross-cutting rule every migrated unit must hold, in prose>",
      "parity-relevant": true
    }
  ],
  "rule-checks": [
    {
      "id": "<RULE_CHECK_ID: stable identifier, e.g. rc-names>",
      "asserts": "<INVARIANT_ID: the id of the invariant this check enforces>",
      "check": "<FILL: the mechanical assertion run against the changed code>",
      "severity": "fail"
    }
  ]
}
