# Extraction override — razor grammar (fixture target)

A real, committed file used as the on-disk target for the `valid-profile.json`
fixture's `extraction-overrides` entry, so the validator's path-existence check
passes for the valid case.

> Fixture support file. Not a production override.
