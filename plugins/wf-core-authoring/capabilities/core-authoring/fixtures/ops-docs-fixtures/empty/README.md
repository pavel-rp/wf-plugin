# empty — a covered folder with no `*.ops.md` at all

DELIBERATELY EMPTY of ops docs. This fixture proves the never-vacuously-green rule: a covered
folder whose target set resolves to zero `*.ops.md` files is an ERROR, not a silent pass. Do not
add an ops doc here.
