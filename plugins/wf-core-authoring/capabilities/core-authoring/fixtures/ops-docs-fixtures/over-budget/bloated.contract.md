# Bloated reference half

Paired reference for `bloated.ops.md`. Carries the counterpart heading so the ONLY thing the
over-budget fixture plants is the line-budget violation.

## Only operation

Rationale, never read at boot.
