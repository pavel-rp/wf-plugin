# locator — rationale

Reproduces PR 370: an authoring-only rationale placed directly under `agents/`, where every file is
auto-discovered as a subagent. It carries no agent frontmatter, so it registers a phantom agent.
