# guide

A balanced fence first:

```text
closed
```

Then one that is never closed:

````markdown
```text
an inner three-backtick fence does not close a four-backtick one
```

Everything from here renders as code.
