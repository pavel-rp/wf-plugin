# Violation fixture — W1, an unowned write claim

Every non-comment, non-blank line below is a planted W1: a write claim on a committed lifecycle
path made on an ordinary skill's own authority, with no resolver lifecycle ownership on the line.

This skill writes its cached verdict to `.wf/install-state.json` when the run completes.

Create `.wf/slots/ship.review.md` from the answers gathered during the interview.

The agent may edit `.wf/slots/spec.publish.md` directly when the composed body looks stale.

Allowed: write any file under `.wf/` that the run needs.

This skill overwrites `.wf/install-state.json` on every invocation.
