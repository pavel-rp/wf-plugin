---
name: slotfree
description: Fixture skill with no declared slots and no markers. Use never — fixture only.
allowed-tools: [Read]
---

# /wf:slotfree — fixture: a slot-free skill

This body declares no slots and carries no `wf:slot` markers, so the lint is
inert on it — exactly like every real skill in the tree today.

## Edge Cases

None — fixture.

```
SLOTFREE — fixture
Next: none — terminus
```
