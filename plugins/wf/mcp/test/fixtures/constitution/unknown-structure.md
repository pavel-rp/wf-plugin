# Project Constitution

**Composed:** 2026-07-21 11:38
**Model:** gpt-5.6-sol[1m]
**Registry:** git, audit, sr

<!-- COMMITTED FIXTURE — a composed record whose structure this composer does not
     recognize: a hand-added `## House rules` section sits between the core
     articles and the capability articles.

     The property it pins is the fail-closed one. A record the composer cannot
     place is REFUSED and left exactly as it is; it is never regenerated, and the
     project's own writing at the end of the file is never the price of an
     unrecognized heading somewhere above it. -->

## Precedence

1. **Project clauses override capability clauses.**

## Core articles (provenance: core)

1. **The spec is the single source of truth.**
2. **No phase skips its gate.** Every phase is a human-approved artifact that feeds the next; nothing advances past an unapproved gate.

## House rules

Hand-added by the project, between two sections the composer owns. It did not author
this, cannot place it, and must not absorb or displace it.

## Capability articles (provenance: each capability)

### sr

- **precommit-self-review:** required

## Project clauses (provenance: project)

1. **house-rules-are-binding:** the section above is not decoration.
