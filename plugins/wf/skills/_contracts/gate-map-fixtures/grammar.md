# Fixture grammar — a sound output block

A synthetic layer's output contract, used only by `gate-map-guard.sh --selftest`.

```text
SAMPLE — <summary>

1. item: <id>
   level: <ok | warn>
   note: <one line>
```
