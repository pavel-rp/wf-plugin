# Fixture registry — `..` segment in Root (fails CHECK 4a)

A `## Plugin Roots` Root must not contain a `..` segment (traversal guard).

## Capabilities

| Capability | Path |
|------------|------|

## Plugin Roots

| Plugin  | Root      |
|---------|-----------|
| testpkg | ../escape |
