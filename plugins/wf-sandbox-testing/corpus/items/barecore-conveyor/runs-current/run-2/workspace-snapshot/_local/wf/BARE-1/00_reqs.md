# BARE-1: Demo work item

> No tracker capability is registered in this fixture, so this is a blank local requirements
> file — the documented silent local-only fallback, not a failed fetch.
> Fetched by: bare-core fixture seed (no tracker read performed)

## Description

A demo objective for the bare-core conveyor fixture. It exists only to give the conveyor
something to spec, plan, decompose, and implement so that all seven declared slots fire.
