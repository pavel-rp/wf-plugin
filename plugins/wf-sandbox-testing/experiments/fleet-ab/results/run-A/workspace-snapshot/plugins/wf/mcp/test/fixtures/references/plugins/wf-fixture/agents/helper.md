# wf-fixture:helper

FIXTURE (WF-354). The agent target that makes `resolvable/SKILL.md`'s
`subagent_type: wf-fixture:helper` reference resolve. Its content is irrelevant;
its existence at `plugins/wf-fixture/agents/helper.md` is the whole point.
