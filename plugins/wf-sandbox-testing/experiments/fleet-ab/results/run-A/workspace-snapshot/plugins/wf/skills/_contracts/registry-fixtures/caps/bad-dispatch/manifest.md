# bad-dispatch capability manifest (fixture — malformed dispatch column, WF-239)

**Kind:** adapter

The dispatch cell lacks an `inline:` / `subagent:` prefix — core cannot reach it.

## Fragments

| phase  | contribution-kind | dispatch     | scope |
|--------|-------------------|--------------|-------|
| verify | finding           | `hooks/x.md` | —     |
