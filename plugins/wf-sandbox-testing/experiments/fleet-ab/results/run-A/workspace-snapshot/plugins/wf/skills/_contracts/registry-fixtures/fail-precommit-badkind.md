# Fixture registry — a fragment at the `pre-commit` seam invents a bespoke `self-review` kind (fails)

## Capabilities

| Capability        | Path                                                                    |
|-------------------|-------------------------------------------------------------------------|
| precommit-badkind | plugins/wf/skills/_contracts/registry-fixtures/caps/precommit-badkind    |
