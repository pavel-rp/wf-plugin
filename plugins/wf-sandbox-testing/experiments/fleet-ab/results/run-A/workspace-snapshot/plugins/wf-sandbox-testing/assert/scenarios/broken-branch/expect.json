{
  "_comment": "The deliberately broken fixture — same structural expectations as demo-branch, but the run outputs violate the terminal-block family (the skill ended with an 'error' status instead of a created/active/switched branch). The tier must produce a RED report naming terminal_block as the failed assertion. Files-touched and ops-invoked still pass, so the red verdict is isolated and named, not a blanket failure.",
  "scenario": "broken-branch",
  "skill": "/wf:branch FAKE-1",
  "families": {
    "terminal_block": {
      "name": "BRANCH",
      "status_ere": "^(created|already-active|switched)$",
      "min_pass_rate": 1.0
    },
    "files_touched": {
      "required_globs": ["_local/fake/op-log.jsonl", "_local/wf/FAKE-1/index.md"],
      "forbidden_globs": ["src/*"],
      "min_pass_rate": 1.0
    },
    "ops_invoked": {
      "required_ops": ["delivery:current-branch-query", "delivery:branch-create"],
      "min_pass_rate": 1.0
    }
  }
}
